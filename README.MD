### Gather the information by providing the link and the the related questions 

### setup 
```bash
 python setup.py install 
 python setup.py sdist 
 pip install .
```

### Install All The Requirements
```bash
pip install -r requirements.txt 
```

